Feature: User actions on top navigation

    Scenario: Click on top menu link (z-23)
    When We click on z-23 link
    Then We can see content about this ship (z-23)

    Scenario: Click on top menu link (z-46)
    When We click on z-46 link
    Then We can see content about this ship (z-46)

    Scenario: Click on top menu link (z-52)
    When We click on z-52 link
    Then We can see content about this ship (z-52)
